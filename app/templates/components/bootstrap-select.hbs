{{#if label}}
<label class="{{if srOnly 'sr-only' ''}} selectize" for="{{inputId}}">{{label}}</label>
{{/if}}
{{#if isEditing}}
{{ember-selectize selection=selection
    content=content
    optionLabelPath=optionLabelPath
    optionValuePath=optionValuePath}}
{{else}}
  {{value}}
{{/if}}
{{label}}