{{#if label}}
<label for="{{hourSelectId}}">{{label}}</label>
{{/if}}
{{#if isEditing}}
<p class="form-inline">
{{bs-datetimepicker date=date mindate=mindate maxdate=maxdate forceDateOutput=true}}
</p>
{{else}}
  {{moment date "dddd HH:mm"}}
{{/if}}