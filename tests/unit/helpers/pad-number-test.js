import {
  padNumber
} from '../../../helpers/pad-number';
import { module, test } from 'qunit';

module('PadNumberHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = padNumber(42);
  assert.ok(result);
});
