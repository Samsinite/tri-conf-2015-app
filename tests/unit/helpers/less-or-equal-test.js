import {
  lessOrEqual
} from '../../../helpers/less-or-equal';
import { module, test } from 'qunit';

module('LessOrEqualHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = lessOrEqual(42);
  assert.ok(result);
});
